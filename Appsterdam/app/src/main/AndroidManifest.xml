<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".AppsterdamApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppSplash"
        tools:targetApi="34">

        <activity
            android:name="rs.appsterdam.app.presentation.main.MainActivity"
            android:exported="true"
            android:theme="@style/Theme.Appsterdam.NoActionBar">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name="rs.appsterdam.app.presentation.settings.SettingsActivity"
            android:exported="true"
            android:label="@string/title_settings"
            android:theme="@style/Theme.Appsterdam" />
    </application>
</manifest>
